import 'package:flutter/material.dart';

class CreateEarning extends StatelessWidget {
  const CreateEarning({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Test");
  }
}
